import React from 'react';
import { Props } from './interface';
import './index.less'

const defaultProps = {

};

const {{name}}: React.FC<Props> = userProps => {
  const props = { ...defaultProps, ...userProps };

  return <div className="{{name}}">{{name}}</div>;
  };

  export default {{name}};
